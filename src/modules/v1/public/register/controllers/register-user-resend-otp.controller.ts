import { Request, Response, NextFunction } from "express";

export const registerUserResendOtpController = async (req: Request, res: Response, next: NextFunction) => {
    try {
        
    } catch (error: unknown) {
        next(error)
    }
}